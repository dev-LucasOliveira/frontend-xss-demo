/** Application route paths (English slugs). */
export const ROUTES = {
  INSECURE: '/insecure',
  SECURE: '/secure',
  /** Legacy Portuguese paths — kept for redirects only */
  LEGACY_INSECURE: '/inseguro',
  LEGACY_SECURE: '/seguro',
} as const
